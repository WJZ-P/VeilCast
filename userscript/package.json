{
  "name": "veilcast-userscript",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check",
    "test": "node --test tests/*.test.mjs ../viewer/*.test.mjs"
  }
}
